import React from 'react'

const channelPage = () => {
    return (
        //its called dynamic routing because when any route we seracrh in url if not found then the folder which is make lik [username] it will run
        
    <div>page</div>
  )
}

export default channelPage